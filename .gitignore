# Dependencies & build output
node_modules/
.next/
dist/
*.tsbuildinfo

# Secrets — NEVER commit these
.env
.env.local
.env.*.local
.env.production
.env.development

# Logs & runtime crud
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
ngrok.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Coverage reports
coverage/

# Database backups
backup-*.gz
*.dump

# Filled-in version of the SSM seed script (contains secrets at runtime).
# Copy scripts/seed-ssm.sh.template -> scripts/seed-ssm.sh, fill, run, then
# leave the filled file local only.
scripts/seed-ssm.sh

# AWS local credential files / SSH keys (defensive)
.aws-credentials
.aws-config
*.pem

# Antigravity temporary workspace artifact folder
d8899fc0-978d-4647-8048-9c214779a913/


